# Artwork boundary

No external artwork is included in Phase 0. Add original or licensed assets with
source and license records here when requested. Import bundled artwork from UI
modules. Future content definitions refer to stable asset IDs, resolved by the
presentation layer; saves and domain logic never store file paths or React nodes.
Do not use Rockstar/GTA assets. Do not add remote fonts or image dependencies by default.
