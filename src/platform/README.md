# Browser adapters — reserved

Future local persistence, save import/export, lifecycle, clock and random-source
adapters belong here. Implement only when requested. Adapters translate external
input into validated domain data; they must not own economic formulas.
